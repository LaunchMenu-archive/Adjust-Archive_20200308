import { ModuleLocation } from "../../../module/_types/ModuleLocation";
/**
 * A type storing all the data of what locations are currently being moved
 */
export declare type LocationsMoveData = {
    locations: ModuleLocation[];
};
